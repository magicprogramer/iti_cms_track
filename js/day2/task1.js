let Name;
while (true)
{
    let reg = new RegExp("^[a-zA-Z]+$");
    Name = prompt("enter your name");
    if (reg.test(Name))
    {
        break;
    }
    
}
while (true)
{
    let reg = new RegExp("^")
}